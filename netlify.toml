[build]
  command = "bundle exec jekyll build && (bundle exec jekyll algolia || echo 'Warning: Algolia indexing skipped or failed')"
  publish = "_site"

[[redirects]]
  from = "/admin/*"
  to = "/admin/index.html"
  status = 200
